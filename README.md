# Ising Phase Classification with PyTorch

This project trains a neural network to classify 2D Ising model configurations as being **below** or **above** the critical temperature \( T_c \).  
The dataset consists of spin configurations stored in `.pt` format, with corresponding labels.